# Default for production builds: the API is served from the same origin, which
# is what happens when FastAPI serves the exported frontend.
# Deploying the frontend separately (Netlify, S3)? Set NEXT_PUBLIC_API_URL in
# that platform's environment settings — a real environment variable takes
# precedence over this file.
NEXT_PUBLIC_API_URL=/api/v1
