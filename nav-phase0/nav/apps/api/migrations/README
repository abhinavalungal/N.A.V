Alembic migrations for N.A.V.

  alembic upgrade head                     apply
  alembic downgrade -1                     roll back one revision
  alembic revision --autogenerate -m "..." generate from ORM models

Phase 0 contains only the baseline revision. Domain tables (users, companies,
vessels, voyages, ...) arrive with Phases 1 and 2.
